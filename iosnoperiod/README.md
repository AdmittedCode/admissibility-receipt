# iOS No-Period Paths

Displayed without the leading period for iOS readability:

`github/workflows/build.yml`

Note: the leading period has been intentionally omitted from the displayed path above.

Canonical path:

`.github/workflows/build.yml`
